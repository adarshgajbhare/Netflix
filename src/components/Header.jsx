import { NETFLIX_LOGO } from "../utils/constant";
const Header = () => {
  return (
    <div className="">
      <img className="h-12" src={NETFLIX_LOGO} />
    </div>
  );
};

export default Header;
